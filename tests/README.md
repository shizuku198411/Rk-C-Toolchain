# Toolchain Tests

Integration and generated-RKX validation tests for Rk-C Toolchain live here.
Test-only executables such as `rkxwritecheck` are packed into the test appfs
image through this integration path and are not distributed in normal images.
